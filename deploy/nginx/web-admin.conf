# web-admin 静态前端（容器内部监听 3002，供 admin-proxy 反代 / -> web-admin:3002）
server {
    listen 3002;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA 回退：adapter-static 以 index.html 作为 fallback，未匹配静态资源时回到入口
    # index.html 必须 no-cache：否则浏览器启发式缓存旧入口，重建后仍加载旧 chunk
    # （旧包烧入的 ADMIN_URL 错误会持续复现，如 custom-domain 误触发）。
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # 构建产物（content-hashed）长缓存
    location /_app/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # gzip
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;
}
